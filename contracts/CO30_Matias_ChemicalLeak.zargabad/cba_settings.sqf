// Dynulo Settings
force persistent_gear_common_enabled = true;   	// Set to 'false' to disable persistent gear for missions where we aren't using our own gear.
force persistent_gear_common_readOnly = false; 	// set to 'true' if the gear should be loaded, but changes are not saved. (Useful for training missions)
